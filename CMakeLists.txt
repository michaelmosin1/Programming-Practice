cmake_minimum_required(VERSION 3.8)
project(bimbim)
enable_language(C)
add_library(sorting sortalg.c)
add_executable(main main.c)
target_compile_options(main PUBLIC "-O2")
target_compile_options(sorting PUBLIC "-O2")
target_link_options(main PUBLIC "-lm")
target_link_libraries(main sorting)